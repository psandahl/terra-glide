#pragma once

// Requests are generated from TerraGlide and are sent to the Kernel.
// Some requests requires a response, but some not.
// Each request type have their own Link.

using StopRequest = int;
